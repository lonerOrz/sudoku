[package]
name = "sudoku-core"
version.workspace = true
edition.workspace = true

[dependencies]
rand = "0.8"
sudoku-solver = { path = "../sudoku-solver" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "sudoku"
harness = false
